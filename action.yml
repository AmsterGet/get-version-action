name: 'Get version'
description: 'Calculates the version type from event'
inputs:
  who-to-greet:  # id of input
    description: 'Who to greet'
    required: true
    default: 'World'
outputs:
  version-type: # id of output
    description: 'The version type'
runs:
  using: 'node12'
  main: 'index.js'
